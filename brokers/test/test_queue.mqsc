DEFINE QLOCAL(TEST.LOCAL.QUEUE) +
       DESCR('Test queue') +
       PUT(ENABLED) +
       GET(ENABLED) + 
       NOTRIGGER +
       MSGDLVSQ(PRIORITY) + 
       MAXDEPTH(5000) +
       MAXMSGL(4194304) +
       USAGE(NORMAL) +
       REPLACE
DISPLAY QLOCAL(TEST.LOCAL.QUEUE)